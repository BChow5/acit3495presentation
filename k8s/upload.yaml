apiVersion: apps/v1
kind: Deployment
metadata:
  name: upload-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: upload-service
  template:
    metadata:
      labels:
        app: upload-service
    spec:
      containers:
        - name: upload-service
          image: us-west1-docker.pkg.dev/acit3495-492606/docker-video-app/upload-service:latest
          ports:
            - containerPort: 8000
          volumeMounts:
            - name: video-storage
              mountPath: /video_storage
      volumes:
        - name: video-storage
          persistentVolumeClaim:
            claimName: video-storage-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: upload-service
spec:
  type: LoadBalancer
  selector:
    app: upload-service
  ports:
    - port: 80
      targetPort: 8000
